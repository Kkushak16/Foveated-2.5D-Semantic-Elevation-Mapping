# Foveated 2.5D LiDAR Grid Mapping — Phase 1 dependencies
# Member A: Ground Segmentation & Dataset Pipeline

# Core
numpy>=1.24
scipy>=1.10

# Visualisation (optional — note: open3d prebuilt wheels currently support Python <= 3.12/3.13)
# open3d>=0.17

# Patchwork++ ground segmentation (preferred back-end)
# Falls back to RANSAC if not installed.
# pypatchworkpp>=0.2
# Uncomment the line above if your platform supports it.
# On Windows you may need to build from source — see:
#   https://github.com/url-kaist/patchwork-plusplus

# Phase 2: Clustering + Classification
scikit-learn>=1.3
joblib>=1.3

# Logging / dev
colorama>=0.4

# Perception training (camera tracking + vehicle info/color)
# ndrplz-style steering CNN needs torch; ahmetozlu-style counting needs opencv.
# All are optional at runtime (stub fallbacks) — install to train for real.
# torch>=2.0
# opencv-python>=4.8
pillow>=9.0
